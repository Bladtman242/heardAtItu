/**
 * This is a sample config file for the project. The real config should be
 * placed as config.json and should have the correct information. The
 * information below is example-only. Additionally, this comment should be
 * removed (as it is not JSON!).
 */
{
    "db": {
        "host": "localhost",
        "username": "heardAtItu",
        "password": "password-lol",
        "database": "heardAtItu"
    },
    "twitter": {
        "OAuth": {
            "consumerKey": "conskey",
            "consumerSecret": "conssec",
            "OAuthToken": "oauhtoke",
            "OAuthSecret": "oauthsec"
        },
        "password": "twitter-password"
    }
    "defaultController": "index"
}